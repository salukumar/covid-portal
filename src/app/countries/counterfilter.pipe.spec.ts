import { CounterfilterPipe } from './counterfilter.pipe';

describe('CounterfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CounterfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
